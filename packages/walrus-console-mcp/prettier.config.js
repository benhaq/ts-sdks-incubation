// Matches the formatting the codebase already uses (previously enforced by
// biome): 2-space indent, double quotes, semicolons, 100-column width.
export default {
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
  semi: true,
  singleQuote: false,
  trailingComma: "all",
};
